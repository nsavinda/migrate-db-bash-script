# Migrate Database Bash Script

## setup

Rename the `example.env` file to `.env` 
Add the necessary details to the .env file.

Ensure the script has the permission to execute:

```bash
chmod +x migrate.sh
```

## import

```bash
./migrate.sh import
```

## dump

```bash
./migrate.sh dump
```